//These are 100% vital to my code.
//(I would lose 3% without them)

//npm init -y
//"type": "module" (in package)
export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}
